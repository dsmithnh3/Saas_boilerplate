{
  "name": "@acme/e2e",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test || echo 'Playwright not installed, skipping E2E tests'"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
